# PostgreSQL connection string
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:masterccm02@localhost:5432/clube_vantagem?schema=public"

# Segredo para assinar tokens JWT (use uma string aleatória forte em produção)
# Gere com: openssl rand -base64 32
JWT_SECRET="troque-este-segredo-por-uma-string-aleatoria-de-32-bytes-ou-mais"

NODE_ENV = 'production'
NEXT_PUBLIC_SITE_URL =

ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_NAME=
